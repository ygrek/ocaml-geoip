
ocaml-geoip
================

OCaml bindings to libgeoip <http://maxmind.com> library.

Build
-----

Bindings code is generated by camlidl and the
generated code is included in source tree, so there
is no need to install camlidl if you only want to build
the library.
Just run `make` and `make install`.
